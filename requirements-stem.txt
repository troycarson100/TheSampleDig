# Python deps for Stem Splitter (scripts/stem-split.py).
# Install: pip install -r requirements-stem.txt
# Demucs will download the htdemucs model on first run.
demucs>=4.0.0
certifi
torchcodec

# Optional: for More Stems > Separate Vocals (lead/backing). Uncomment to install:
# pip install "audio-separator[cpu]"  (or [gpu] for CUDA)
